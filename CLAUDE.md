# Claude 行为准则

## 数据与依据
- **禁止编造数据**。性能数字、延迟、吞吐量等必须来自实测日志、官方文档或可引用的基准测试，不得凭印象估算。
- 不确定时，明确说"没有实测数据，需要实际验证"，而不是给出看似合理的估计值。

## 日志分析
- 用户说"分析日志"时，到当前项目的 `logs/` 目录下读取**时间戳最新**的日志文件进行分析。
- 用户指明了具体日志文件时，读取指定文件。

## Bug 分析
- 用户提出问题描述或修复方案时，不要直接接受，先深入思考**根本原因**，再结合整体架构判断方案是否正确、是否有副作用，然后给出结论。
- **禁止打补丁**。修复 bug 必须找到根本原因并从根本上解决，不得在症状处加 if/fallback/特殊处理来绕过问题。
